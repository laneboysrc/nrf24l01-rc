- Check SPI timing against data sheet

- Hop every 5ms even if no data received

- Pre-processor output of raw data

- Implement watchdog and brown-out reset

- Check we we can use 750ns Timer instead of 1us to remove conversion losses from 8051

- Use 16 MHz crystal
