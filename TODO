- Startup delay after power-on before talking to the NRF chip

- Check SPI timing against data sheet

- Hop every 5ms even if no data received

- Pre-processor output, and output of raw data

- Persistent storage for bind data

